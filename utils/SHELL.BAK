int main() {
	char *a = "command:\0";
	char *buf = "                                                                                                               ";
	char in;
	int i;

	do {
		printstr("\r\n");
		printstr(a);
		i=0;
		while ((in = getchar()) != 13) {
			mputchar(in);
			buf[i] = in;
			i++;
		}
		printstr("\r\n");
		buf[i] = '\0';
		printstr("you said: '");
		printstr(buf);
		printstr("'");
	} while (strcmp(buf,"exit\0") == -1);
	return 0;
}
