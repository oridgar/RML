#include "mystdio.h"

void main() {
	char *str1 = "init ";
	char *str2 = "shell";
	/*
	int i = 0;
	while (str1[i] != 0) {
		mputchar(str1[i]);
		i++;
	} */

	list_root_files();
	//printstr(str1);
	//printstr(str2);
	printstr("\r\n");
	//return 0;
}
